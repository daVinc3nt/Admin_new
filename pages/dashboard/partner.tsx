import type { NextPage } from "next";
import PartnerMenu from "@/components/Partner/Partner";
const Partner: NextPage = () => {
  return (
    <div className="w-full no-scrollbar">
      <PartnerMenu />
    </div>
  );
};

export default Partner;
